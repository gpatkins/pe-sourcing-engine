{
    # Global options
    admin off
}

# Main application (v5.7 test instance)
# Change this to your domain or use IP:port
:80 {
    reverse_proxy app:8000
    
    # Security headers
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
    
    # Logging
    log {
        output stdout
    }
}

# If you want to use a domain, uncomment and configure:
# your-domain.com {
#     reverse_proxy app:8000
#     
#     header {
#         Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
#         X-Content-Type-Options "nosniff"
#         X-Frame-Options "SAMEORIGIN"
#         Referrer-Policy "strict-origin-when-cross-origin"
#     }
#     
#     log {
#         output file /data/access.log
#     }
# }
